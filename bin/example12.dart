void main(List<String> args) {
  /// Type promotion is possible
  /// Let's say, we need a more precise variable for age

  final double hisAge = 30;
  print(hisAge);

  /// final int herAge = 40.5;
  /// Demoting is not allowed
}
