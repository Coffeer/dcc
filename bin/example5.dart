void main(List<String> args) {
  final age = 10;

  const age1 = 11;

// Finals can be assigned a constant value
  final age2 = age1;

// Constants cannot be assigned a non-constant value,
// so the following is an invalid operation
// const ag3 = age;
}
