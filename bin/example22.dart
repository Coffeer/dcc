void main(List<String> args) {
  const String? name = null;
  print(name);
  print(null);
}

/// Opntional Values in Dart
/// Optionality in Dart is the ability to have a value or not have a value
/// Either a variable contains a value or it doesn't (null)